class Solution {
public:
    void reverseString(vector<char>& s) {
        int no_it=(s.size())/2;
        int n=s.size();
        for(int i=0;i<no_it;i++){
            swap(s[i],s[n-1-i]);
        }
        
    }
};
